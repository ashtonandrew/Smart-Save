# --- OpenAI ---
OPENAI_API_KEY=put-your-key-here
# Good defaults; change if you prefer
OPENAI_MODEL=gpt-4.1-mini

# Optional: if you’re using a compatible endpoint (Azure, etc.)
# OPENAI_BASE_URL=https://your-endpoint/v1

# Default province tag to stamp on rows (used only if you don’t pass --province)
PROVINCE_DEFAULT=AB
# --- OpenAI ---
OPENAI_API_KEY=put-your-key-here
# Good defaults; change if you prefer
OPENAI_MODEL=gpt-4.1-mini

# Optional: if you’re using a compatible endpoint (Azure, etc.)
# OPENAI_BASE_URL=https://your-endpoint/v1

# Default province tag to stamp on rows (used only if you don’t pass --province)
PROVINCE_DEFAULT=AB
